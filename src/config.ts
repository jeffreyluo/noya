export const FR_MAP = {
    'React' : 'react',
    'Vue' : 'vue'
};

export const UI_MAP = {
    'Antd' : 'antd'
}

export const CGI = {
    GET_CONFIG : `${location.protocol}//your-service-address/noya-services/config/getter`,
    REPORT : `${location.protocol}//your-service-address/noya-services/es/report`,
};

export const ERR = {
    CGI : 'request cgi error'
}

export const MODE_KEY = 'mode';
export const MODE_CONFIG = 'config';
export const MODE_REPORT = 'report';